# mf6Voronoi Studio — runtime dependencies
#
# Install with:  pip install -r requirements.txt
#
# NOTE: mf6Voronoi currently pins numpy<2.2.5. If another package pulls a newer
# numpy, re-pin it (pip install "numpy<2.2.5") or the mesh engine may error.

mf6Voronoi>=0.0.37     # the meshing engine this GUI drives
PyQt5>=5.15            # desktop GUI toolkit + native QGraphicsView map canvas
Pillow>=10.0            # PNG/JPG background image reading
geopandas>=0.13        # shapefile I/O and geometry handling
shapely>=2.0           # geometry primitives
fiona>=1.9             # vector I/O (used by mf6Voronoi and geopandas)
pyproj>=3.5            # CRS handling / unit validation
numpy>=1.26,<2.2.5     # pinned for mf6Voronoi compatibility
pandas>=1.5            # used by the GUI for interactive layer editing
flopy>=3.6             # writes the ModelMuse-importable MODFLOW 6 DISV model
rasterio>=1.3          # GeoTIFF background images AND per-layer top/botm sampling

# Pulled in automatically by mf6Voronoi (listed for completeness):
# scipy, tqdm, pyvista, vtk
#   (pyvista/vtk are optional for this GUI — mf6voronoi_bootstrap.py stubs them
#    if missing, so you can skip them for a lighter install/executable.)

# --- Optional, only needed to BUILD the standalone executable ---
# pyinstaller>=6.0
